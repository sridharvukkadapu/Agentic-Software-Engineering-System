["Should resolution attempts on expired links be silently dropped from all analytics, or should they be recorded as a separate, distinct event type (e.g., 'expired_hit') visible in the dashboard or raw event log?","Is click recording currently done synchronously within the request handler (before or after the expiry check), or asynchronously via a queue/event stream? The answer determines whether the fix is a code-order change or requires filtering at the consumer level.","Are there any downstream systems (billing, rate-limiting, partner reporting) that consume click events and would be affected by this change in recording behavior?","What is the desired behavior if a link expires at the exact moment of a concurrent request — should the system favor consistency (never count if expired) or availability (accept a small window of over-counting)? Is an atomic check-and-record operation required?"]