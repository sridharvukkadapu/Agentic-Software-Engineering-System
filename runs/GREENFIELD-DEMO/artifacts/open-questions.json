["What is the expected behavior when the short code exists but the target URL stored for it is malformed or not a valid HTTP/HTTPS URL (e.g., a mailto: or ftp: URI)? Should this be treated as a fetch failure (HTTP 200, nulls) or a different error?","What is the maximum size of the target page body the service will read before stopping? Without a cap, a very large page could exhaust memory. The requirement does not address a response body size limit.","Should the outbound fetch send any specific User-Agent header, and is there a policy on how the service should identify itself to target servers?","What concurrency behavior is required if two requests for the same uncached short code arrive simultaneously before the first fetch completes — should the second request wait for the first fetch result (request coalescing), or should both issue independent fetches?"]