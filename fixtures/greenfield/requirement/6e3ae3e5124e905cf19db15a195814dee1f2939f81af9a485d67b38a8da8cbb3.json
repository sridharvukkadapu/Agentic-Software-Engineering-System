{"request":{"systemPrompt":"You are a requirements analyst for a URL shortener service. You read a raw\nrequirement (a feature request or a bug report) and normalize it into a\nstructured specification an engineering team can act on.\n\nYou must be honest about what the requirement does not say. Do not invent\nbehavior the requirement never specifies. If wording is unclear or leaves room\nfor more than one reasonable interpretation, record that in \"ambiguities\".\nIf the requirement is entirely silent about something the design will need an\nanswer to before implementation can proceed (not just unclear, but never\naddressed at all), record that separately in \"openQuestions\" and do not guess\nat a policy to fill it.\n\nRespond with exactly one fenced json block containing an object with these keys:\n- normalizedProblem: string, the requirement restated precisely and concisely\n- acceptanceCriteria: array of objects, each with \"id\" (string like \"AC-1\"),\n  \"description\" (string), and \"riskLevel\" (one of \"LOW\", \"MEDIUM\", \"HIGH\", \"CRITICAL\")\n- ambiguities: array of strings, wording or scope that is unclear\n- assumptions: array of strings, reasonable assumptions you are proposing to\n  resolve an ambiguity (for a human to confirm or reject, not silently adopted)\n- outOfScope: array of strings, explicitly excluded from this requirement\n- openQuestions: array of strings, things the requirement never addresses at all\n  that the design will need answered before implementation can proceed. Leave\n  this empty if the requirement, even if imperfectly worded, actually covers\n  everything the design needs.\n","userPrompt":"Here is the raw requirement:\n\n# Requirement: Link preview endpoint\n\nAdd an endpoint to the URL shortener that returns a preview (title and description) for\na given short code, so a client can show what a shortened link points to before\nfollowing it.\n\n- `GET /api/v1/urls/{code}/preview` returns the target URL's page title and meta\n  description, resolved from the long URL the short code points to.\n- The preview must be cached, since fetching the target page on every request would be\n  slow and would hammer whatever site the link points to.\n- Fetching an external page must not hang the request indefinitely if the target is slow\n  or unreachable; apply a timeout and return a sensible partial or empty result rather\n  than blocking.\n- If the short code does not exist, return 404, consistent with the existing lookup and\n  redirect endpoints.\n\nThis is new functionality on the URL shortener; no existing preview mechanism exists to\nbuild on.\n","maxTokens":2000,"nodeId":"REQUIREMENT"},"response":{"text":"```json\n{\n  \"normalizedProblem\": \"Add a GET /api/v1/urls/{code}/preview endpoint that fetches and returns the HTML page title and meta description of the long URL a short code resolves to, with caching and a timeout on the upstream fetch. Returns 404 for unknown short codes and a partial or empty result when the target page is slow or unreachable.\",\n  \"acceptanceCriteria\": [\n    {\n      \"id\": \"AC-1\",\n      \"description\": \"GET /api/v1/urls/{code}/preview returns HTTP 200 with a JSON body containing at least a 'title' field and a 'description' field when the short code exists and the target page is reachable and parseable.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-2\",\n      \"description\": \"GET /api/v1/urls/{code}/preview returns HTTP 404 when the short code does not exist in the data store, consistent with the existing lookup and redirect endpoints.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-3\",\n      \"description\": \"The 'title' field in the response is populated from the HTML <title> element of the resolved target page when present.\",\n      \"riskLevel\": \"MEDIUM\"\n    },\n    {\n      \"id\": \"AC-4\",\n      \"description\": \"The 'description' field in the response is populated from the HTML <meta name=\\\"description\\\"> content attribute of the resolved target page when present.\",\n      \"riskLevel\": \"MEDIUM\"\n    },\n    {\n      \"id\": \"AC-5\",\n      \"description\": \"If the target page does not contain a <title> element, the 'title' field in the response is null or an empty string rather than causing an error.\",\n      \"riskLevel\": \"LOW\"\n    },\n    {\n      \"id\": \"AC-6\",\n      \"description\": \"If the target page does not contain a <meta name=\\\"description\\\"> element, the 'description' field in the response is null or an empty string rather than causing an error.\",\n      \"riskLevel\": \"LOW\"\n    },\n    {\n      \"id\": \"AC-7\",\n      \"description\": \"A configurable timeout is applied to the outbound HTTP fetch of the target page. When the timeout elapses before the response is fully received, the endpoint returns a non-5xx response (the exact status code and body are to be decided per open questions) with 'title' and 'description' both null or empty, rather than hanging or returning a 5xx.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-8\",\n      \"description\": \"When the target URL is unreachable (DNS failure, connection refused, TLS error, etc.), the endpoint returns a non-hanging response with 'title' and 'description' both null or empty rather than returning a 5xx or blocking.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-9\",\n      \"description\": \"Preview data is served from cache on subsequent requests for the same short code, so the target page is not re-fetched on every call.\",\n      \"riskLevel\": \"CRITICAL\"\n    },\n    {\n      \"id\": \"AC-10\",\n      \"description\": \"The target long URL is resolved from the short code using the same data store as the existing lookup/redirect endpoints; the preview endpoint does not maintain its own URL mapping.\",\n      \"riskLevel\": \"MEDIUM\"\n    },\n    {\n      \"id\": \"AC-11\",\n      \"description\": \"HTTP redirects encountered while fetching the target page are followed, up to a reasonable maximum, so the final page's metadata is returned rather than redirect HTML.\",\n      \"riskLevel\": \"MEDIUM\"\n    }\n  ],\n  \"ambiguities\": [\n    \"The requirement says 'sensible partial or empty result' on timeout/unreachability but does not specify whether the HTTP status code should be 200 (with empty fields), 206, 503, or something else.\",\n    \"It is unclear whether a failed or timed-out upstream fetch should itself be cached (to avoid hammering an unreachable host) or whether only successful fetches should be cached.\",\n    \"The requirement does not specify what HTTP method or content-type the outbound fetch should use (e.g., whether to send Accept: text/html, a realistic User-Agent string, etc.), which can affect whether the target site returns useful HTML.\",\n    \"It is ambiguous whether 'meta description' means only <meta name=\\\"description\\\"> or also Open Graph (<meta property=\\\"og:description\\\">) and similar alternatives, and whether there is a priority order.\",\n    \"The requirement does not define what 'title' means when the target URL responds with non-HTML content (PDF, image, JSON, etc.).\",\n    \"It is not stated whether the endpoint should follow the short code's own redirect chain (i.e., if the stored long URL is itself a redirect) or stop at the stored URL.\"\n  ],\n  \"assumptions\": [\n    \"ASSUMPTION-1: The response body is JSON with at least { \\\"title\\\": string|null, \\\"description\\\": string|null, \\\"targetUrl\\\": string } — the targetUrl field is included because returning only metadata without the URL would make the preview less useful, though the requirement never mentions it. Needs human confirmation.\",\n    \"ASSUMPTION-2: Only <meta name=\\\"description\\\"> is considered for the description field; Open Graph and other meta variants are out of scope unless confirmed otherwise.\",\n    \"ASSUMPTION-3: A default outbound fetch timeout of 5 seconds is a reasonable starting point; the exact value should be configurable without a code change (e.g., via environment variable).\",\n    \"ASSUMPTION-4: Failed/timed-out fetches are also cached for a shorter TTL (e.g., 60 seconds) to prevent the endpoint from being used to hammer unreachable hosts on every client poll. Needs human confirmation.\",\n    \"ASSUMPTION-5: Up to 5 HTTP redirects are followed during the outbound fetch before treating it as a failure.\",\n    \"ASSUMPTION-6: The endpoint is read-only and unauthenticated, consistent with the existing redirect endpoint behavior described in the requirement. Needs human confirmation.\"\n  ],\n  \"outOfScope\": [\n    \"Generating or storing Open Graph images or any visual preview beyond title and description text.\",\n    \"Proactively pre-warming the preview cache when a new short code is created.\",\n    \"Any write operations (creating, updating, or deleting short codes or preview data).\",\n    \"Any existing preview mechanism (the requirement explicitly states none exists).\",\n    \"Webhooks or push notifications when preview metadata changes on the target page.\"\n  ],\n  \"openQuestions\": [\n    \"OQ-1: What is the cache TTL for successful preview fetches? The requirement mandates caching but never specifies how long data should be considered fresh.\",\n    \"OQ-2: What cache backend should be used (in-process memory, Redis, database table, etc.)? The existing service's infrastructure may constrain this.\",\n    \"OQ-3: Should preview metadata be refreshed/invalidated when the short code's target URL is updated, or does it simply expire after TTL?\",\n    \"OQ-4: Is there a maximum length for the returned title and description strings, and should they be truncated server-side?\",\n    \"OQ-5: Should the endpoint be rate-limited (per IP, per API key, or globally) to prevent abuse, given that each unique cache-miss causes an outbound HTTP request?\",\n    \"OQ-6: What should the response status code be specifically when the short code exists but the upstream fetch fails or times out — 200 with empty fields, or a distinct error code (e.g., 422, 503)?\",\n    \"OQ-7: Are there any target URL schemes or domains that must be blocked from being fetched (e.g., private RFC-1918 addresses, localhost, internal services) to prevent SSRF attacks?\"\n  ]\n}\n```","inputTokens":588,"outputTokens":1893,"latencyMillis":34948}}