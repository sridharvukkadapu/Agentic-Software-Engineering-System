{"request":{"systemPrompt":"You are a software architect producing a design specification for a change to a\nURL shortener service, based on a normalized requirement and an impact analysis.\n\nPropose a specific technical approach. Name at least one alternative you\nconsidered and rejected, and say why you rejected it: a design with no\nconsidered alternative is not defensible.\n\nRespond with exactly one fenced json block containing an object with these keys:\n- classStructure: array of strings, one per class/component to add or change,\n  each naming the class and its responsibility\n- apiContract: string, an OpenAPI-fragment-style description of any new or\n  changed endpoints (path, method, request/response shape)\n- dataModelChanges: array of strings, schema or entity changes required\n- chosenApproach: string, the approach you are recommending\n- rejectedAlternatives: array of objects, each with \"alternative\" (string) and\n  \"reason\" (string) for why it was not chosen\n- affectsCriteria: array of strings, the acceptance criterion ids (like \"AC-1\")\n  this design decision affects\n","userPrompt":"Produce a design specification for the change described below.\n\nRequirement: Add GET /api/v1/urls/{code}/preview returning a cached title and description for the target URL, with a timeout on the external fetch and a 404 for unknown codes.\n\n","maxTokens":3000,"nodeId":"DESIGN"},"response":{"text":"```json\n{\n  \"classStructure\": [\n    \"PreviewController: HTTP handler for GET /api/v1/urls/{code}/preview; validates the short code exists, delegates to PreviewService, maps results to HTTP responses (200, 404, 503)\",\n    \"PreviewService: Orchestrates preview retrieval; checks PreviewCache first, falls back to MetadataFetcher, writes results back to cache, enforces overall SLA timeout\",\n    \"MetadataFetcher: Performs the external HTTP GET against the resolved target URL, parses <title> and <meta name='description'> from the HTML head, respects a configurable per-fetch timeout and follows up to N redirects\",\n    \"PreviewCache: Read/write abstraction over a Redis (or in-process fallback) store; keys on short code; stores serialized PreviewDocument with a configurable TTL\",\n    \"PreviewDocument: Value object / entity holding: shortCode (string), targetUrl (string), title (string|null), description (string|null), fetchedAt (ISO-8601), cacheExpiresAt (ISO-8601)\",\n    \"UrlRepository (existing, modified): Add lookupByCode(code) → UrlRecord|null used by PreviewService to resolve the target URL and confirm the code exists\",\n    \"PreviewConfig: Configuration value object exposing fetchTimeoutMs (default 3000), cacheTimeoutMs (default 3000), cacheTtlSeconds (default 86400), maxRedirects (default 5)\"\n  ],\n  \"apiContract\": \"paths:\\n  /api/v1/urls/{code}/preview:\\n    get:\\n      summary: Return cached title and description for the target URL of a short code\\n      parameters:\\n        - name: code\\n          in: path\\n          required: true\\n          schema:\\n            type: string\\n            example: aB3xQ\\n      responses:\\n        '200':\\n          description: Preview metadata (may be partial if title or description absent on target page)\\n          content:\\n            application/json:\\n              schema:\\n                type: object\\n                required: [shortCode, targetUrl, fetchedAt]\\n                properties:\\n                  shortCode:\\n                    type: string\\n                    example: aB3xQ\\n                  targetUrl:\\n                    type: string\\n                    format: uri\\n                    example: https://example.com/some/article\\n                  title:\\n                    type: string\\n                    nullable: true\\n                    example: Example Domain\\n                  description:\\n                    type: string\\n                    nullable: true\\n                    example: An example article about something\\n                  fetchedAt:\\n                    type: string\\n                    format: date-time\\n                  cacheExpiresAt:\\n                    type: string\\n                    format: date-time\\n        '404':\\n          description: Short code not found\\n          content:\\n            application/json:\\n              schema:\\n                type: object\\n                properties:\\n                  error:\\n                    type: string\\n                    example: Short code 'aB3xQ' not found\\n        '503':\\n          description: External fetch timed out or failed; no cached preview available\\n          content:\\n            application/json:\\n              schema:\\n                type: object\\n                properties:\\n                  error:\\n                    type: string\\n                    example: Preview unavailable: upstream fetch timed out\\n                  retryAfter:\\n                    type: integer\\n                    description: Suggested seconds before retry\\n                    example: 30\",\n  \"dataModelChanges\": [\n    \"New Redis key schema: preview:{code} → JSON-serialised PreviewDocument; TTL set to cacheTtlSeconds (default 86400 s); atomic SET NX used on first write to prevent stampede\",\n    \"New RDBMS table 'url_previews' (optional persistent fallback): columns: short_code VARCHAR PK, target_url TEXT NOT NULL, title TEXT NULL, description TEXT NULL, fetched_at TIMESTAMPTZ NOT NULL, cache_expires_at TIMESTAMPTZ NOT NULL; index on cache_expires_at for background eviction jobs\",\n    \"No changes to existing 'urls' / 'short_links' table schema; UrlRepository gains a read-only lookupByCode query against that existing table\"\n  ],\n  \"chosenApproach\": \"Cache-aside with Redis and a bounded external fetch: PreviewController receives the request and calls PreviewService. PreviewService first calls PreviewCache.get(code); on a cache hit it returns the stored PreviewDocument immediately. On a cache miss it calls UrlRepository.lookupByCode(code) — returning 404 if absent — then calls MetadataFetcher.fetch(targetUrl) wrapped in a timeout (fetchTimeoutMs). On success the document is written to PreviewCache with a TTL and returned as a 200. On timeout/network failure with no cached data a 503 is returned; if a stale cached entry exists it is returned with a staleness flag rather than a 503 ('stale-while-revalidate' semantics). All timeout values and the cache TTL are externalised in PreviewConfig so they can be tuned without a code change. MetadataFetcher only downloads the HTML head section (HTTP Range or stream-and-abort after </head>) to minimise bandwidth. This approach keeps latency predictable, protects the service from slow external sites, and avoids redundant fetches for popular codes.\",\n  \"rejectedAlternatives\": [\n    {\n      \"alternative\": \"Eager pre-fetch at shortening time: when a new short URL is created, immediately fetch and store the preview metadata in the background\",\n      \"reason\": \"Adds latency/complexity to the URL-creation critical path and wastes resources for codes that are never previewed. It also does not handle codes created before this feature ships, requiring a backfill job. Cache-aside fetches on demand and only for codes that actually receive preview requests.\"\n    },\n    {\n      \"alternative\": \"No caching — always fetch from the target URL on each preview request\",\n      \"reason\": \"Makes response time entirely dependent on external site performance, violates the stated requirement for caching, risks rate-limiting or blocking by target hosts under even moderate traffic, and exposes the service to SSRF amplification without the blast-radius reduction that a cached path provides.\"\n    },\n    {\n      \"alternative\": \"Use a third-party link-preview SaaS API (e.g. LinkPreview.net) instead of in-house MetadataFetcher\",\n      \"reason\": \"Introduces an external runtime dependency and potential data-privacy concerns (all target URLs are sent to a third party). It also adds per-request cost and an additional failure domain. An internal fetcher is straightforward to implement given that only <title> and <meta description> are needed, and it keeps the service self-contained.\"\n    },\n    {\n      \"alternative\": \"Store preview data as a JSON column on the existing urls/short_links table rather than a separate cache layer\",\n      \"reason\": \"Conflates ephemeral, high-churn cache data with durable link-record data, complicates the existing table's write path, and makes TTL-based eviction awkward in a relational store. A dedicated Redis cache provides O(1) TTL management and keeps the URL table unchanged.\"\n    }\n  ],\n  \"affectsCriteria\": [\n    \"AC-1\",\n    \"AC-2\",\n    \"AC-3\",\n    \"AC-4\"\n  ]\n}\n```","inputTokens":306,"outputTokens":1789,"latencyMillis":36327}}