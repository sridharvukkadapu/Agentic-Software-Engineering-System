{"request":{"systemPrompt":"You are a requirements analyst for a URL shortener service. You read a raw\nrequirement (a feature request or a bug report) and normalize it into a\nstructured specification an engineering team can act on.\n\nYou must be honest about what the requirement does not say. Do not invent\nbehavior the requirement never specifies. If wording is unclear or leaves room\nfor more than one reasonable interpretation, record that in \"ambiguities\".\nIf the requirement is entirely silent about something the design will need an\nanswer to before implementation can proceed (not just unclear, but never\naddressed at all), record that separately in \"openQuestions\" and do not guess\nat a policy to fill it.\n\nRespond with exactly one fenced json block containing an object with these keys:\n- normalizedProblem: string, the requirement restated precisely and concisely\n- acceptanceCriteria: array of objects, each with \"id\" (string like \"AC-1\"),\n  \"description\" (string), and \"riskLevel\" (one of \"LOW\", \"MEDIUM\", \"HIGH\", \"CRITICAL\")\n- ambiguities: array of strings, wording or scope that is unclear\n- assumptions: array of strings, reasonable assumptions you are proposing to\n  resolve an ambiguity (for a human to confirm or reject, not silently adopted)\n- outOfScope: array of strings, explicitly excluded from this requirement\n- openQuestions: array of strings, things the requirement never addresses at all\n  that the design will need answered before implementation can proceed. Leave\n  this empty if the requirement, even if imperfectly worded, actually covers\n  everything the design needs.\n","userPrompt":"Here is the raw requirement:\n\n# Requirement: Link preview endpoint\n\nAdd an endpoint to the URL shortener that returns a preview (title and description) for\na given short code, so a client can show what a shortened link points to before\nfollowing it.\n\n- `GET /api/v1/urls/{code}/preview` returns the target URL's page title and meta\n  description, resolved from the long URL the short code points to.\n- The preview must be cached, since fetching the target page on every request would be\n  slow and would hammer whatever site the link points to.\n- Fetching an external page must not hang the request indefinitely if the target is slow\n  or unreachable; apply a timeout and return a sensible partial or empty result rather\n  than blocking.\n- If the short code does not exist, return 404, consistent with the existing lookup and\n  redirect endpoints.\n\nThis is new functionality on the URL shortener; no existing preview mechanism exists to\nbuild on.\n","maxTokens":2000,"nodeId":"REQUIREMENT"},"response":{"text":"```json\n{\n  \"normalizedProblem\": \"Add a GET /api/v1/urls/{code}/preview endpoint that fetches the HTML page pointed to by the short code's target URL, extracts the page title and meta description, caches the result, and returns it to the caller so they can preview a link before following it. If the short code is unknown the endpoint returns 404. External fetching must be time-bounded to avoid hanging the request.\",\n\n  \"acceptanceCriteria\": [\n    {\n      \"id\": \"AC-1\",\n      \"description\": \"GET /api/v1/urls/{code}/preview returns HTTP 200 with a JSON body containing at least a 'title' field and a 'description' field when the short code exists and the target page is reachable.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-2\",\n      \"description\": \"The 'title' field in the response is populated from the target page's <title> element.\",\n      \"riskLevel\": \"MEDIUM\"\n    },\n    {\n      \"id\": \"AC-3\",\n      \"description\": \"The 'description' field in the response is populated from the target page's <meta name='description'> content attribute.\",\n      \"riskLevel\": \"MEDIUM\"\n    },\n    {\n      \"id\": \"AC-4\",\n      \"description\": \"When the short code does not exist in the system, the endpoint returns HTTP 404, consistent with the existing lookup and redirect endpoints.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-5\",\n      \"description\": \"Preview data for a given short code is cached after the first successful (or partially successful) fetch, so that subsequent requests for the same code do not trigger a new external HTTP request.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-6\",\n      \"description\": \"A timeout is applied to the outbound HTTP request to the target URL. If the target does not respond within the timeout, the endpoint returns a response (HTTP 200 or a defined partial-result status) with empty or null values for 'title' and/or 'description' rather than blocking indefinitely.\",\n      \"riskLevel\": \"CRITICAL\"\n    },\n    {\n      \"id\": \"AC-7\",\n      \"description\": \"If the target URL is unreachable (DNS failure, connection refused, etc.), the endpoint returns a sensible partial or empty result rather than a 5xx error, analogous to the timeout behavior.\",\n      \"riskLevel\": \"HIGH\"\n    },\n    {\n      \"id\": \"AC-8\",\n      \"description\": \"The endpoint follows the same {code} path-parameter convention as the existing lookup and redirect endpoints.\",\n      \"riskLevel\": \"LOW\"\n    },\n    {\n      \"id\": \"AC-9\",\n      \"description\": \"The outbound fetch request identifies itself with a recognizable User-Agent so target servers can identify and manage the scraper traffic.\",\n      \"riskLevel\": \"MEDIUM\"\n    }\n  ],\n\n  \"ambiguities\": [\n    \"The requirement says 'return a sensible partial or empty result' on timeout or unreachability, but does not specify the HTTP status code for that response (200 with nulls, 206, 422, 503, etc.).\",\n    \"It is unclear whether the cache should be invalidated or refreshed, and if so under what conditions (TTL expiry, manual purge, short-code deletion, target URL update).\",\n    \"The requirement does not define what 'cached' means in terms of storage — in-memory, Redis, database, CDN, etc.\",\n    \"It is unspecified whether the endpoint should follow HTTP redirects when fetching the target URL (some long URLs may themselves redirect).\",\n    \"The requirement does not clarify how to handle target pages that return non-HTML content types (PDFs, images, JSON APIs). It is unclear whether to attempt extraction, return empty fields, or return an error.\",\n    \"It is ambiguous whether both 'title' and 'description' must always be present in the response body (possibly null/empty) or whether absent fields may be omitted entirely.\",\n    \"The requirement does not specify the exact timeout value, nor whether it should be configurable.\",\n    \"It is unclear whether the response should include the resolved target URL itself alongside the preview metadata.\"\n  ],\n\n  \"assumptions\": [\n    \"The HTTP response status for a timed-out or unreachable fetch will be 200 with null/empty 'title' and 'description' fields, so clients receive a consistent schema and can distinguish the partial result from a code-not-found (404).\",\n    \"Cache entries will have a time-to-live (TTL) after which the next request triggers a fresh external fetch; the TTL value will be defined during implementation and should be configurable.\",\n    \"The endpoint will follow a reasonable number of HTTP redirects (e.g., up to 5) when fetching the target URL.\",\n    \"Non-HTML responses will yield null/empty 'title' and 'description' without causing the endpoint to return an error status.\",\n    \"Both 'title' and 'description' fields will always be present in the response body, using null when the value cannot be determined.\",\n    \"The timeout value will be configurable via service configuration rather than hard-coded, with a sensible default (e.g., 5 seconds) chosen during implementation.\",\n    \"The preview fetch is performed lazily on first request rather than eagerly at short-code creation time.\",\n    \"Failed fetches (timeout, unreachable) will also be cached for a shorter negative-cache TTL to prevent repeated hammering of a broken target.\"\n  ],\n\n  \"outOfScope\": [\n    \"Open Graph (og:title, og:description, og:image) or Twitter Card metadata extraction — only <title> and <meta name='description'> are in scope.\",\n    \"Thumbnail or screenshot generation for the target URL.\",\n    \"Any preview mechanism triggered at short-code creation time (eager/background prefetch).\",\n    \"Modifying, creating, or deleting short codes via this endpoint.\",\n    \"Authentication or authorization for accessing the preview endpoint (not mentioned in the requirement).\",\n    \"Any existing preview mechanism (the requirement explicitly states none exists).\"\n  ],\n\n  \"openQuestions\": [\n    \"What is the authoritative cache storage layer available in this service's infrastructure (in-process memory, Redis, a database table, etc.)?\",\n    \"Should cache entries be invalidated or purged when the underlying short code is deleted or its target URL is updated? If so, which system is responsible for triggering that invalidation?\",\n    \"What are the acceptable rate-limiting or robots.txt compliance requirements, if any, when the service fetches external pages?\",\n    \"Is there a security review needed for Server-Side Request Forgery (SSRF) risks, given that this endpoint causes the server to make outbound HTTP requests to user-supplied URLs? If so, is there an allowlist/denylist of target domains or IP ranges required?\",\n    \"Should the preview response include the resolved (long) target URL so clients can display it alongside the title and description?\",\n    \"What HTTP status code should be returned when the short code exists but the external fetch fails (timeout, DNS failure, non-2xx from target)?\"\n  ]\n}\n```","inputTokens":588,"outputTokens":1712,"latencyMillis":33717}}